node_modules/
dist/
# Local D1 and R2 state, created by `wrangler dev` and the --local migrations.
.wrangler/
.DS_Store
*.local
tsconfig.tsbuildinfo

# Reference and sample photographs of objects. Kept out of the repo because
# they are large, the app doesn't read them, and anything committed here is
# permanent — a photo of a filled-in worksheet would carry donor details.
images/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Credentials. .dev.vars is wrangler's local secrets file; the deployed Worker
# reads the same names from `wrangler secret put`. Neither belongs in git, and an
# API key committed once is an API key that has to be rotated.
.dev.vars
.dev.vars.*
*.key

# Never commit real catalogue exports — they can contain collection and donor information.
*.catalogue.json
catalogue-*.json

# Working files from the eHive image conversion. Regenerate with
# scripts/attach-ehive-images.mjs; the masters live outside this repo.
.ehive-jpeg/
